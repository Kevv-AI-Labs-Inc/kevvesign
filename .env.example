NODE_ENV=development
PORT=4100
DOCUMENSO_BASE_URL=http://localhost:3469
ESIGN_DATABASE_URL=postgres://local_user:local_password@127.0.0.1:5432/esign_bridge
# Generate a separate random 32-byte hex key; do not use the example in production.
ESIGN_CREDENTIAL_KEY=
# JSON array: id, SHA-256 keyHash, exact portalOrigin, random callbackSecret (>=32 chars).
ESIGN_PORTAL_CLIENTS_JSON=[]
ESIGN_WEBHOOK_SECRET=
ESIGN_RECONCILE_INTERVAL_MS=60000
